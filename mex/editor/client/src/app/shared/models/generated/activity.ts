/* eslint-disable @typescript-eslint/naming-convention */
// AUTO-GENERATED by mex_form_codegen -- do not edit by hand.

import { z } from "zod";

import {
  identifierInPrimarySourcePattern,
  identifierPattern,
  LinkSchema,
  PublishingTargetSchema,
  TextSchema,
  ThemeSchema,
  yearMonthDayPattern,
  yearMonthPattern,
  yearPattern,
} from "./shared";

/**
 * Zod schema for the ActivityType enum.
 */
export const ActivityTypeSchema = z.enum([
  "https://mex.rki.de/item/activity-type-1",
  "https://mex.rki.de/item/activity-type-3",
  "https://mex.rki.de/item/activity-type-6",
]);

/**
 * TypeScript type inferred from ActivityTypeSchema.
 */
export type ActivityType = z.infer<typeof ActivityTypeSchema>;

/**
 * Zod schema for AdditiveActivity.
 */
export const AdditiveActivitySchema = z.object({
  contact: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  responsibleUnit: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  title: z.array(TextSchema).optional(),
  abstract: z.array(TextSchema).optional(),
  activityType: z.array(ActivityTypeSchema).optional(),
  alternativeTitle: z.array(TextSchema).optional(),
  documentation: z.array(LinkSchema).optional(),
  end: z
    .array(
      z.union([
        z.string().regex(new RegExp(yearMonthDayPattern)),
        z.string().regex(new RegExp(yearMonthPattern)),
        z.string().regex(new RegExp(yearPattern)),
      ]),
    )
    .optional(),
  externalAssociate: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  funderOrCommissioner: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  fundingProgram: z.array(z.string()).optional(),
  involvedPerson: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  involvedUnit: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  isPartOfActivity: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  publication: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  relatedActivity: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  shortName: z.array(TextSchema).optional(),
  start: z
    .array(
      z.union([
        z.string().regex(new RegExp(yearMonthDayPattern)),
        z.string().regex(new RegExp(yearMonthPattern)),
        z.string().regex(new RegExp(yearPattern)),
      ]),
    )
    .optional(),
  succeeds: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  theme: z.array(ThemeSchema).optional(),
  website: z.array(LinkSchema).optional(),
  $type: z.literal("AdditiveActivity"),
  supersededBy: z.string().regex(new RegExp(identifierPattern)).nullish(),
});

/**
 * TypeScript type inferred from AdditiveActivitySchema.
 */
export type AdditiveActivity = z.infer<typeof AdditiveActivitySchema>;

/**
 * Zod schema for PreventiveActivity.
 */
export const PreventiveActivitySchema = z.object({
  $type: z.literal("PreventiveActivity"),
  abstract: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  activityType: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  alternativeTitle: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  contact: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  documentation: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  end: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  externalAssociate: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  funderOrCommissioner: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  fundingProgram: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  involvedPerson: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  involvedUnit: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  isPartOfActivity: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  publication: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  responsibleUnit: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  relatedActivity: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  shortName: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  start: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  succeeds: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  theme: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  title: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  website: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
});

/**
 * TypeScript type inferred from PreventiveActivitySchema.
 */
export type PreventiveActivity = z.infer<typeof PreventiveActivitySchema>;

/**
 * Zod schema for SubtractiveActivity.
 */
export const SubtractiveActivitySchema = z.object({
  contact: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  responsibleUnit: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  title: z.array(TextSchema).optional(),
  abstract: z.array(TextSchema).optional(),
  activityType: z.array(ActivityTypeSchema).optional(),
  alternativeTitle: z.array(TextSchema).optional(),
  documentation: z.array(LinkSchema).optional(),
  end: z
    .array(
      z.union([
        z.string().regex(new RegExp(yearMonthDayPattern)),
        z.string().regex(new RegExp(yearMonthPattern)),
        z.string().regex(new RegExp(yearPattern)),
      ]),
    )
    .optional(),
  externalAssociate: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  funderOrCommissioner: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  fundingProgram: z.array(z.string()).optional(),
  involvedPerson: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  involvedUnit: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  isPartOfActivity: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  publication: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  relatedActivity: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  shortName: z.array(TextSchema).optional(),
  start: z
    .array(
      z.union([
        z.string().regex(new RegExp(yearMonthDayPattern)),
        z.string().regex(new RegExp(yearMonthPattern)),
        z.string().regex(new RegExp(yearPattern)),
      ]),
    )
    .optional(),
  succeeds: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  theme: z.array(ThemeSchema).optional(),
  website: z.array(LinkSchema).optional(),
  $type: z.literal("SubtractiveActivity"),
});

/**
 * TypeScript type inferred from SubtractiveActivitySchema.
 */
export type SubtractiveActivity = z.infer<typeof SubtractiveActivitySchema>;

/**
 * Zod schema for WorkflowActivity.
 */
export const WorkflowActivitySchema = z.object({
  forbiddenPublishingTarget: z.array(PublishingTargetSchema).optional(),
  $type: z.literal("WorkflowActivity"),
});

/**
 * TypeScript type inferred from WorkflowActivitySchema.
 */
export type WorkflowActivity = z.infer<typeof WorkflowActivitySchema>;

/**
 * Zod schema for ActivityRuleSetRequest.
 */
export const ActivityRuleSetRequestSchema = z.object({
  additive: AdditiveActivitySchema.optional(),
  subtractive: SubtractiveActivitySchema.optional(),
  preventive: PreventiveActivitySchema.optional(),
  workflow: WorkflowActivitySchema.optional(),
  $type: z.literal("ActivityRuleSetRequest"),
});

/**
 * TypeScript type inferred from ActivityRuleSetRequestSchema.
 */
export type ActivityRuleSetRequest = z.infer<typeof ActivityRuleSetRequestSchema>;

/**
 * Zod schema for ActivityRuleSetResponse.
 */
export const ActivityRuleSetResponseSchema = z.object({
  additive: AdditiveActivitySchema.optional(),
  subtractive: SubtractiveActivitySchema.optional(),
  preventive: PreventiveActivitySchema.optional(),
  workflow: WorkflowActivitySchema.optional(),
  $type: z.literal("ActivityRuleSetResponse"),
  stableTargetId: z.string().regex(new RegExp(identifierPattern)),
});

/**
 * TypeScript type inferred from ActivityRuleSetResponseSchema.
 */
export type ActivityRuleSetResponse = z.infer<typeof ActivityRuleSetResponseSchema>;

/**
 * Zod schema for BaseActivity.
 */
export const BaseActivitySchema = z.object({
  contact: z.array(z.string().regex(new RegExp(identifierPattern))).min(1),
  responsibleUnit: z.array(z.string().regex(new RegExp(identifierPattern))).min(1),
  title: z.array(TextSchema).min(1),
  abstract: z.array(TextSchema).optional(),
  activityType: z.array(ActivityTypeSchema).optional(),
  alternativeTitle: z.array(TextSchema).optional(),
  documentation: z.array(LinkSchema).optional(),
  end: z
    .array(
      z.union([
        z.string().regex(new RegExp(yearMonthDayPattern)),
        z.string().regex(new RegExp(yearMonthPattern)),
        z.string().regex(new RegExp(yearPattern)),
      ]),
    )
    .optional(),
  externalAssociate: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  funderOrCommissioner: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  fundingProgram: z.array(z.string()).optional(),
  involvedPerson: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  involvedUnit: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  isPartOfActivity: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  publication: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  relatedActivity: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  shortName: z.array(TextSchema).optional(),
  start: z
    .array(
      z.union([
        z.string().regex(new RegExp(yearMonthDayPattern)),
        z.string().regex(new RegExp(yearMonthPattern)),
        z.string().regex(new RegExp(yearPattern)),
      ]),
    )
    .optional(),
  succeeds: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  theme: z.array(ThemeSchema).optional(),
  website: z.array(LinkSchema).optional(),
});

/**
 * TypeScript type inferred from BaseActivitySchema.
 */
export type BaseActivity = z.infer<typeof BaseActivitySchema>;

/**
 * Zod schema for ExtractedActivity (extends BaseActivity).
 */
export const ExtractedActivitySchema = BaseActivitySchema.extend({
  hadPrimarySource: z.string().regex(new RegExp(identifierPattern)),
  identifierInPrimarySource: z
    .string()
    .regex(new RegExp(identifierInPrimarySourcePattern))
    .min(1)
    .max(1000),
  $type: z.literal("ExtractedActivity"),
});

/**
 * TypeScript type inferred from ExtractedActivitySchema.
 */
export type ExtractedActivity = z.infer<typeof ExtractedActivitySchema>;

/**
 * Zod schema for MergedActivity (extends BaseActivity).
 */
export const MergedActivitySchema = BaseActivitySchema.extend({
  $type: z.literal("MergedActivity"),
  identifier: z.string().regex(new RegExp(identifierPattern)),
  supersededBy: z.string().regex(new RegExp(identifierPattern)).nullish(),
});

/**
 * TypeScript type inferred from MergedActivitySchema.
 */
export type MergedActivity = z.infer<typeof MergedActivitySchema>;

/**
 * Zod schema for PreviewActivity.
 */
export const PreviewActivitySchema = z.object({
  contact: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  responsibleUnit: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  title: z.array(TextSchema).optional(),
  abstract: z.array(TextSchema).optional(),
  activityType: z.array(ActivityTypeSchema).optional(),
  alternativeTitle: z.array(TextSchema).optional(),
  documentation: z.array(LinkSchema).optional(),
  end: z
    .array(
      z.union([
        z.string().regex(new RegExp(yearMonthDayPattern)),
        z.string().regex(new RegExp(yearMonthPattern)),
        z.string().regex(new RegExp(yearPattern)),
      ]),
    )
    .optional(),
  externalAssociate: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  funderOrCommissioner: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  fundingProgram: z.array(z.string()).optional(),
  involvedPerson: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  involvedUnit: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  isPartOfActivity: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  publication: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  relatedActivity: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  shortName: z.array(TextSchema).optional(),
  start: z
    .array(
      z.union([
        z.string().regex(new RegExp(yearMonthDayPattern)),
        z.string().regex(new RegExp(yearMonthPattern)),
        z.string().regex(new RegExp(yearPattern)),
      ]),
    )
    .optional(),
  succeeds: z.array(z.string().regex(new RegExp(identifierPattern))).optional(),
  theme: z.array(ThemeSchema).optional(),
  website: z.array(LinkSchema).optional(),
  $type: z.literal("PreviewActivity"),
  identifier: z.string().regex(new RegExp(identifierPattern)),
  supersededBy: z.string().regex(new RegExp(identifierPattern)).nullish(),
});

/**
 * TypeScript type inferred from PreviewActivitySchema.
 */
export type PreviewActivity = z.infer<typeof PreviewActivitySchema>;
