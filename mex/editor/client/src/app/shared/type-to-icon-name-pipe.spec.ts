import { TypeToIconNamePipe } from "./type-to-icon-name-pipe";

describe("TypeToIconNamePipe", () => {
  it("create an instance", () => {
    const pipe = new TypeToIconNamePipe();
    expect(pipe).toBeTruthy();
  });
});
